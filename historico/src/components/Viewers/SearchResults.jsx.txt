import React from 'react';

const SearchResults = ({ results, onSelect }) => {
  return (
    <div className="bg-finder-window p-6 rounded-lg">
      <h2 className="text-xl font-semibold text-finder-text mb-4">
        Resultados da Busca
      </h2>
      <div className="space-y-4">
        {results.map((result, index) => (
          <div
            key={index}
            className="p-4 bg-finder-hover rounded-lg hover:bg-finder-hover/80 transition-colors cursor-pointer"
            onClick={() => onSelect(result)}
          >
            <h3 className="text-finder-text font-medium">{result.title}</h3>
            <p className="text-finder-text-secondary text-sm mt-1">
              {result.description}
            </p>
            <div className="flex items-center mt-2 text-finder-text-secondary text-sm">
              <span className="mr-2">🔍</span>
              <span>{result.source}</span>
            </div>
          </div>
        ))}
      </div>
    </div>
  );
};

export default SearchResults; 