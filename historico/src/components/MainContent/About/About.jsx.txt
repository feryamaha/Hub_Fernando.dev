import React from 'react';

const About = () => {
  const experiences = [
    {
      period: '2020 - Presente',
      role: 'Desenvolvedor Full Stack Senior',
      company: 'Tech Solutions',
      description: 'Desenvolvimento de soluções web escaláveis e manutenção de sistemas legados.',
    },
    {
      period: '2018 - 2020',
      role: 'Desenvolvedor Front-end',
      company: 'Creative Agency',
      description: 'Criação de interfaces responsivas e implementação de designs modernos.',
    },
  ];

  const education = [
    {
      period: '2016 - 2020',
      degree: 'Bacharelado em Ciência da Computação',
      institution: 'Universidade Federal',
    },
    {
      period: '2021',
      degree: 'Especialização em Desenvolvimento Web',
      institution: 'Instituto de Tecnologia',
    },
  ];

  return (
    <div className="space-y-8">
      <section>
        <h2 className="text-3xl font-bold text-finder-text mb-6">Sobre Mim</h2>
        <div className="bg-finder-sidebar rounded-lg p-6 shadow-lg">
          <p className="text-finder-text-secondary mb-4">
            Sou um desenvolvedor apaixonado por tecnologia e inovação, com mais de 5 anos
            de experiência no desenvolvimento de aplicações web. Minha jornada começou com
            front-end e evoluiu para full stack, sempre buscando as melhores práticas e
            tecnologias mais recentes.
          </p>
        </div>
      </section>

      <section>
        <h3 className="text-2xl font-bold text-finder-text mb-4">Experiência Profissional</h3>
        <div className="space-y-4">
          {experiences.map((exp, index) => (
            <div key={index} className="bg-finder-sidebar rounded-lg p-6 shadow-lg">
              <div className="flex justify-between items-start mb-2">
                <div>
                  <h4 className="text-lg font-semibold text-finder-text">{exp.role}</h4>
                  <p className="text-finder-accent">{exp.company}</p>
                </div>
                <span className="text-finder-text-secondary">{exp.period}</span>
              </div>
              <p className="text-finder-text-secondary">{exp.description}</p>
            </div>
          ))}
        </div>
      </section>

      <section>
        <h3 className="text-2xl font-bold text-finder-text mb-4">Formação</h3>
        <div className="space-y-4">
          {education.map((edu, index) => (
            <div key={index} className="bg-finder-sidebar rounded-lg p-6 shadow-lg">
              <div className="flex justify-between items-start">
                <div>
                  <h4 className="text-lg font-semibold text-finder-text">{edu.degree}</h4>
                  <p className="text-finder-accent">{edu.institution}</p>
                </div>
                <span className="text-finder-text-secondary">{edu.period}</span>
              </div>
            </div>
          ))}
        </div>
      </section>
    </div>
  );
};

export default About; 