@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  body {
    @apply bg-finder-window text-finder-text font-sans antialiased;
  }
}

@layer components {
  .window-control {
    @apply w-3 h-3 rounded-full cursor-pointer transition-opacity duration-200;
  }

  .window-control-close {
    @apply bg-finder-control-close shadow-control hover:opacity-80;
  }

  .window-control-minimize {
    @apply bg-finder-control-minimize shadow-control hover:opacity-80;
  }

  .window-control-maximize {
    @apply bg-finder-control-maximize shadow-control hover:opacity-80;
  }

  .sidebar-item {
    @apply flex items-center px-4 py-2 text-finder-text hover:bg-finder-hover rounded-lg cursor-pointer transition-colors duration-200;
  }

  .sidebar-icon {
    @apply w-4 h-4 mr-3 text-finder-text-secondary;
  }

  .w-sidebar {
    @apply w-[200px];
  }

  .scrollbar-finder {
    @apply scrollbar-thin scrollbar-thumb-finder-text-secondary scrollbar-track-finder-window;
  }
}

/* Temas */
.theme-red {
  --finder-accent: #FF5F57;
}

.theme-orange {
  --finder-accent: #FEBC2E;
}

.theme-yellow {
  --finder-accent: #FFE08C;
}

.theme-green {
  --finder-accent: #28C840;
}

.theme-blue {
  --finder-accent: #0A84FF;
}

.theme-purple {
  --finder-accent: #BF5AF2;
}

.theme-gray {
  --finder-accent: #98989D;
}

/* Estilos específicos do Finder */
.bg-finder-window {
  background-color: #1E1E1E;
}

.bg-finder-sidebar {
  background-color: #252526;
}

.text-finder {
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
}

.bg-finder-hover {
  background-color: rgba(255, 255, 255, 0.1);
}

.bg-finder-search {
  background-color: rgba(255, 255, 255, 0.1);
} 