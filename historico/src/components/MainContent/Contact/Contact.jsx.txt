import React, { useState } from 'react';
import { EnvelopeIcon, PhoneIcon, MapPinIcon } from '@heroicons/react/24/outline';

const Contact = () => {
  const [formData, setFormData] = useState({
    name: '',
    email: '',
    message: '',
  });

  const handleSubmit = (e) => {
    e.preventDefault();
    // Implementar lógica de envio do formulário
    console.log('Formulário enviado:', formData);
  };

  const handleChange = (e) => {
    const { name, value } = e.target;
    setFormData(prev => ({
      ...prev,
      [name]: value
    }));
  };

  return (
    <div className="space-y-8">
      <section>
        <h2 className="text-3xl font-bold text-finder-text mb-6">Contato</h2>
        <div className="bg-finder-sidebar rounded-lg p-6 shadow-lg">
          <p className="text-finder-text-secondary mb-4">
            Entre em contato comigo através do formulário abaixo ou pelos meios de
            contato listados. Estou sempre aberto a novas oportunidades e colaborações.
          </p>
        </div>
      </section>

      <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
        <section>
          <h3 className="text-2xl font-bold text-finder-text mb-4">Informações de Contato</h3>
          <div className="space-y-4">
            <div className="flex items-center text-finder-text-secondary">
              <EnvelopeIcon className="w-5 h-5 mr-2" />
              <span>seu.email@exemplo.com</span>
            </div>
            <div className="flex items-center text-finder-text-secondary">
              <PhoneIcon className="w-5 h-5 mr-2" />
              <span>+55 (11) 99999-9999</span>
            </div>
            <div className="flex items-center text-finder-text-secondary">
              <MapPinIcon className="w-5 h-5 mr-2" />
              <span>São Paulo, SP - Brasil</span>
            </div>
          </div>
        </section>

        <section>
          <h3 className="text-2xl font-bold text-finder-text mb-4">Envie uma Mensagem</h3>
          <form onSubmit={handleSubmit} className="space-y-4">
            <div>
              <label htmlFor="name" className="block text-finder-text mb-1">Nome</label>
              <input
                type="text"
                id="name"
                name="name"
                value={formData.name}
                onChange={handleChange}
                className="w-full bg-finder-window text-finder-text rounded-md px-4 py-2 focus:outline-none focus:ring-1 focus:ring-finder-accent"
                required
              />
            </div>
            <div>
              <label htmlFor="email" className="block text-finder-text mb-1">Email</label>
              <input
                type="email"
                id="email"
                name="email"
                value={formData.email}
                onChange={handleChange}
                className="w-full bg-finder-window text-finder-text rounded-md px-4 py-2 focus:outline-none focus:ring-1 focus:ring-finder-accent"
                required
              />
            </div>
            <div>
              <label htmlFor="message" className="block text-finder-text mb-1">Mensagem</label>
              <textarea
                id="message"
                name="message"
                value={formData.message}
                onChange={handleChange}
                rows="4"
                className="w-full bg-finder-window text-finder-text rounded-md px-4 py-2 focus:outline-none focus:ring-1 focus:ring-finder-accent"
                required
              />
            </div>
            <button
              type="submit"
              className="bg-finder-accent text-finder-text px-6 py-2 rounded-md hover:bg-finder-accent-hover transition-colors"
            >
              Enviar Mensagem
            </button>
          </form>
        </section>
      </div>
    </div>
  );
};

export default Contact; 